import React from 'react';
import { useParams } from 'react-router-dom';

const Profile = () => {
    // const { id } = useParams();

    return (
        <div data-testid="profile-component">Profile page</div>
    )
}

export default Profile; 
